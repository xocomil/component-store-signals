import { PrimaryBorderDirective } from './primary-border.directive';

describe('PrimaryBorderDirective', () => {
  it('should create an instance', () => {
    const directive = new PrimaryBorderDirective();
    expect(directive).toBeTruthy();
  });
});
